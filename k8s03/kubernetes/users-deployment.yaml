apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
        - name: users
          image: xuerong/kub-demo-users:latest
          env:
            - name: AUTH_ADDRESS
              value: localhost  #Investigating: If I have one more container running in same pod, with same url
        - name: auth
          image: xuerong/kub-demo-auth:latest